@AGENTS.md

# Marios Pogiatzeas — Personal Portfolio

## 1. Purpose

A personal portfolio website that is shown **alongside the CV when applying for jobs**. It is a
professional credibility artifact, not an experiment. Every decision — copy, motion, spacing,
performance — is judged by one question: *does this make a hiring manager or recruiter take Marios
more seriously in the 30 seconds they will spend here?*

- **Audience:** recruiters, engineering managers, IT managers, potential clients.
- **Tone:** confident, concrete, understated. No buzzword soup, no "passionate rockstar ninja".
- **Language:** **English only.** All UI copy, code, comments, commit messages and content are in
  English. (The repo owner communicates in Greek, but nothing Greek ships to the browser.)
- **Primary conversion goals:** download the CV, read the work history, contact by email/LinkedIn.

## 2. Tech Stack

| Concern | Choice | Notes |
|---|---|---|
| Framework | **Next.js 16.3** (App Router) | See `AGENTS.md` — this is NOT the Next.js in your training data. Read `node_modules/next/dist/docs/` before using an API. |
| Language | **TypeScript** (strict) | Path alias `@/*` → `src/*`. |
| Styling | **Tailwind CSS v4** | CSS-first config in `src/app/globals.css` via `@theme`. **No `tailwind.config.js`.** |
| Animation | **Framer Motion** (`motion` package v13, imported from `motion/react`) | The only animation library. Do not add GSAP, AOS, react-spring, etc. |
| Theming | **next-themes** | `class` strategy, light + dark, no flash on load. |
| Icons | **lucide-react** | Only icon set. **Never use emoji as icons.** |
| Fonts | `next/font/google` — **Space Grotesk** (display) + **Inter** (body/UI) | Self-hosted at build time. |
| Deploy target | Vercel (static-friendly, no backend yet) | Contact form is `mailto:` until a backend exists. |

Package manager: **npm** (`package-lock.json` is committed).

```bash
npm run dev     # dev server
npm run build   # production build — must pass before anything is "done"
npm run lint    # eslint
```

## 3. Project Structure

```
portofolio/
├── CLAUDE.md                  # this file
├── AGENTS.md                  # Next.js 16 warning (auto-generated by `next dev`, keep it)
├── .claude/skills/            # UI UX Pro Max design-intelligence skill (see §9)
├── public/
│   ├── CV/CV_Marios Pogiatzeas_IT-1.pdf   # the real CV — source of truth for content
│   └── image/MariosPhoto.png              # 2048×2048 portrait, used in the hero
└── src/
    ├── app/
    │   ├── layout.tsx         # fonts, metadata, ThemeProvider, Navbar, Footer
    │   ├── page.tsx           # single-page composition of all sections
    │   └── globals.css        # Tailwind v4 theme, design tokens, base styles
    ├── components/
    │   ├── navbar.tsx         # sticky top nav + mobile menu + theme toggle
    │   ├── footer.tsx         # copyright + colophon
    │   ├── section.tsx        # shared section shell: anchor id, rhythm, heading entrance
    │   ├── theme-provider.tsx # next-themes wrapper ("use client")
    │   ├── theme-toggle.tsx   # light/dark switch
    │   ├── icons/github.tsx   # lucide v1 dropped brand marks, so this one is local
    │   └── sections/
    │       ├── hero.tsx       # intro copy (left) + animated portrait (right)
    │       ├── work.tsx       # professional experience timeline
    │       ├── projects.tsx   # selected projects
    │       ├── about.tsx      # bio, skills, education
    │       └── contact.tsx    # email + links + CV download
    └── lib/
        ├── content.ts         # ALL site content as typed data (single source of truth)
        └── motion.ts          # shared Framer Motion variants + timing tokens
```

### Structural rules

- **Content lives in `src/lib/content.ts`, never hardcoded inside JSX.** Updating the CV means
  editing one file. Sections map over the data.
- One section = one file in `src/components/sections/`, default-exported, named after its `id`.
- Section wrappers own their vertical rhythm and `id`; children never set page-level padding.
- Anything using hooks, `motion`, or browser APIs starts with `"use client"`. Everything else stays a
  Server Component. Keep client boundaries as low in the tree as possible.

## 4. Section IDs & Navigation Contract

The site is a **single scrolling page**. Nav links are in-page anchors, and these IDs are a contract —
renaming one means updating `NAV_LINKS` in `src/lib/content.ts` at the same time.

| Nav label | Anchor | Section component |
|---|---|---|
| Work | `#work` | `sections/work.tsx` |
| Projects | `#projects` | `sections/projects.tsx` |
| About | `#about` | `sections/about.tsx` |
| Contact | `#contact` | `sections/contact.tsx` |

Navbar layout: **name on the left** (`Marios Pogiatzeas`, links to `#top`), **nav links + theme
toggle on the right**. Below `md` the links collapse into a hamburger menu.

Smooth scrolling is done with CSS (`scroll-behavior: smooth` + `scroll-margin-top` on sections) so it
degrades gracefully and respects `prefers-reduced-motion`. Do not hand-roll a JS scroll animation.
Every section must clear the sticky navbar — that is what `scroll-margin-top` is for.

## 5. Design System

Defined once in `src/app/globals.css` under `@theme`. **Never write raw hex values in components** —
use the semantic Tailwind classes generated from the tokens.

### Palette

Near-monochrome warm neutrals with a single teal accent used sparingly (links, active states, one CTA
per screen). Deliberately avoids the overused purple/blue "AI gradient" look.

| Token | Light | Dark | Use |
|---|---|---|---|
| `background` | `#FAFAF9` | `#0B0B0C` | page background |
| `surface` | `#FFFFFF` | `#141416` | cards, panels |
| `border` | `#E7E5E4` | `#26262A` | hairlines, dividers |
| `foreground` | `#18181B` | `#FAFAF9` | primary text |
| `muted` | `#57534E` | `#A1A1AA` | secondary text (passes 4.5:1 on both) |
| `accent` | `#0F766E` | `#2DD4BF` | links, active nav, primary CTA |

Dark mode is a **separately tuned tonal palette**, not an inversion. Both modes must be checked for
contrast; the design is never "done" in only one mode.

### Typography

- Display / headings: **Space Grotesk**, weight 500–700, tight tracking (`-0.02em`).
- Body / UI: **Inter**, weight 400–500, `line-height: 1.6`, max line length ~70ch.
- Scale: `12 / 14 / 16 / 18 / 20 / 24 / 32 / 44 / 64`. Body is **never below 16px**.
- Exactly one `h1` per page (the hero name/role). Heading levels never skip.

### Layout

- Container: `max-w-6xl` with `px-6 md:px-10`, centred.
- Spacing follows a 4px scale — use Tailwind's default steps, no arbitrary values like `mt-[37px]`.
- Section rhythm: `py-24 md:py-32`.
- Breakpoints checked every time: **375 / 768 / 1024 / 1440**. No horizontal scroll, ever.
- Use `min-h-dvh` rather than `min-h-screen` for full-height areas (mobile browser chrome).

## 6. Motion Guidelines (Framer Motion)

Motion here signals craft, so it must feel intentional and calm — never showy.

```ts
import { motion } from "motion/react"   // NOT "framer-motion"
```

Shared variants and timing tokens live in `src/lib/motion.ts`. Use them instead of inventing new
durations per component — the whole site should share one rhythm.

**Rules:**

- Animate **`transform` and `opacity` only.** Never animate `width`, `height`, `top`, or `left`.
- Duration **150–300ms** for micro-interactions, **≤ 500ms** for entrances. Exits run ~60–70% of the
  enter duration.
- Easing: `ease-out` for entering, `ease-in` for exiting. Prefer springs for physical elements
  (the hero portrait, hover lifts).
- Entrances use `whileInView` with `viewport={{ once: true, amount: 0.25 }}` — content must never
  stay invisible if the observer misfires.
- Stagger list/grid children by **40–60ms**. Cap it: at most 1–2 focal animated elements per view.
- **`prefers-reduced-motion` is mandatory.** Use Framer Motion's `useReducedMotion()` and fall back
  to a plain fade or no motion at all. This is an accessibility requirement, not a nicety.
- No parallax, no scroll-jacking, no marquees, no typewriter effects.
- Animation never blocks input, and never delays the first paint of real content.

### Hero animation (the site's one signature moment)

Left column: eyebrow → name → role → short bio → CTAs, staggered upward fade (`y: 16 → 0`).
Right column: the portrait scales/fades in (`scale: 0.94 → 1`, spring, slight delay) inside a rounded
frame with a soft accent glow, plus a very subtle continuous float. The float must be disabled under
reduced motion.

## 7. Content — Source of Truth

`public/CV/CV_Marios Pogiatzeas_IT-1.pdf` is authoritative. **Never invent employers, dates, metrics,
or technologies.** If something isn't in the CV and the owner hasn't stated it, ask — don't fill it in.

### Identity

- **Name:** Marios Pogiatzeas
- **Email:** `marios1522@hotmail.com`
- **GitHub:** https://github.com/marios22132-cell (no LinkedIn on file yet)
- **Headline:** Full-Stack Developer & IT Service Desk Administrator
- **Positioning:** builds and ships web applications, and keeps enterprise systems running — the rare
  overlap of product engineering and real infrastructure/support experience.

### Experience (most recent first)

1. **Software Developer Intern — Central Bank of Cyprus** · Jun 2026 – Present
   Market research and comparative evaluation of enterprise Data Catalog solutions for the
   organisation's Data Democratisation initiative. Built a scoring matrix and cost-deployment
   analysis covering technical strengths, weaknesses and implementation risks per platform.
   Delivered a final market research report and an executive presentation proposing a high-level
   target architecture for centralised data discovery and access management.
2. **IT Service Desk Administrator — XM** · Mar 2026 – Jun 2026
   Administered Windows, Azure Portal and GSuite environments; installed and configured hardware and
   software. Ensured seamless daily operation of MT4/MT5 trading platforms through routine system
   audits and proactive conflict resolution. Managed the corporate ticketing system and monitoring
   tools, delivering support and training to staff under strict SLAs.
3. **Web Developer Intern — Eadvertise** · Dec 2025 – Mar 2026
   Designed and developed end-to-end web applications with React, Node.js and Tailwind CSS. Led the
   technical implementation of the **thisIsPaphos** project across the full lifecycle — architecture,
   database design, deployment.
4. **IT Officer — University of Neapolis** · Oct 2022 – Dec 2025
   1st-level support for staff and associates; local network monitoring and system maintenance;
   participated in IT infrastructure upgrade and optimisation projects; user and access-rights
   administration via Active Directory.

### Education

**BSc Applied Computer Science** — Neapolis University Pafos, School of Economics, Business and
Computer Sciences. Thesis: investigation, comprehension and theoretical evaluation of the Transformer
architecture and its evolution from *Attention Is All You Need* to the contemporary variants used in
modern AI systems.

### Skills

- **Web:** React, Next.js, Tailwind CSS, React Native, Expo
- **Backend & Data:** Node.js, MongoDB, PostgreSQL, SQL
- **Programming Languages:** JavaScript, TypeScript, Python, Java
- **Infrastructure:** Windows, macOS, Linux, Azure Portal, Nginx, GSuite
- **Tooling:** Git, GitHub, Docker, Jira, ticketing & monitoring tools
- **Spoken:** Greek (native), English

### Certifications

- The Complete JavaScript Course: From Zero to Expert — Jonas Schmedtmann
- The Complete Web Development Course — Hitesh Choudhary

### Projects

Public repos live at https://github.com/marios22132-cell — that account is the source of truth for
anything with a `repo` link.

1. **Kanaria Developers** — corporate site for a luxury real estate firm.
   Next.js, TypeScript, Tailwind, Framer Motion, Google Maps, Resend.
   [Live](https://kanaria-developers-web-site.vercel.app) ·
   [repo](https://github.com/marios22132-cell/Kanaria-Developers-Web-Site)
2. **MMA Gym Platform** — trainer booking + e-commerce + role-based dashboards.
   Next.js, TypeScript, PostgreSQL, Prisma, NextAuth, Zod.
   [repo](https://github.com/marios22132-cell/Mma_e-comerce_Web-app)
3. **Enterprise Data Catalog Evaluation** — Central Bank of Cyprus. Internal, no public link.
4. **thisIsPaphos** — full-stack platform built at Eadvertise. Client work, source not public.
5. **Backend Mastery** — Node/Express/MongoDB API: JWT auth, uploads, transactional email.
   [repo](https://github.com/marios22132-cell/backend-mastery)
6. **LSTM vs Transformer** — the practical counterpart to the thesis. Python, PyTorch.
   [repo](https://github.com/marios22132-cell/lstm-transformer_comparison)

Add new projects to `PROJECTS` in `src/lib/content.ts`. Each needs: title, role/context, 1–2 sentence
outcome-focused description, tech tags, and then either a `demo`/`repo` link **or** a `note`
explaining why there isn't one — a card with no links and no note reads as unfinished. Prefer
*what it achieved* over *what it used*.

## 8. Quality Bar

Nothing is "done" until `npm run build` passes clean and the checklist below holds.

**Accessibility (non-negotiable):**
- Text contrast ≥ 4.5:1 in **both** light and dark mode.
- Visible focus rings on every interactive element — never `outline: none` without a replacement.
- Full keyboard navigation; tab order matches visual order.
- `aria-label` on every icon-only button (theme toggle, menu toggle).
- Descriptive `alt` text on meaningful images; `alt=""` on decorative ones.
- `prefers-reduced-motion` respected everywhere.

**Performance:**
- Always `next/image` with explicit `width`/`height` (or `fill` + sized parent) to keep CLS < 0.1.
- Hero portrait is the LCP element: mark it `priority`, everything else lazy-loads.
- No layout shift from fonts (`display: swap` via `next/font`).

**Craft:**
- `cursor-pointer` on everything clickable; hover states transition in 150–300ms.
- Responsive at 375 / 768 / 1024 / 1440 with no horizontal scroll.
- Real metadata: title, description, Open Graph, favicon. This link gets pasted into applications.
- Every section works at all breakpoints in both themes before it ships.

**Never:**
- Emoji used as icons.
- Placeholder or lorem-ipsum text left in a commit.
- Invented CV facts.
- Raw hex colours inside components.
- A second animation or CSS library.

## 9. Design Skill

The **UI UX Pro Max** skill is installed at `.claude/skills/`. Consult it for UI structure, colour,
typography, layout and motion decisions — especially before adding a new section or restyling an
existing one. It provides ranked design rules and a pre-delivery checklist that this project's
quality bar is aligned with.

## 10. Working Agreements

- Respond to the repo owner in **Greek**; write all code, comments and UI copy in **English**.
- Build incrementally, section by section, and verify each in the browser before moving on.
- Consult `node_modules/next/dist/docs/` before reaching for a Next.js API from memory.
- Don't add dependencies without a clear reason — the stack above is deliberately small.
- Keep `AGENTS.md` in the repo; `next dev` regenerates it and deleting it just recreates a diff.
